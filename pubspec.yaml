name: signup
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^1.24.0  # Downgraded version to match the constraint of cloud_firestore
  firebase_auth: ^3.3.0
  cloud_firestore: ^3.1.5
  provider: ^5.0.0 # Replace latest_version with the specific version you want

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
